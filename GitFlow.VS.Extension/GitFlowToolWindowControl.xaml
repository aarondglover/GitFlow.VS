<UserControl x:Class="GitFlowVS.Extension.GitFlowToolWindowControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:GitFlowVS.Extension.UI"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="400"
             Background="{DynamicResource {x:Static SystemColors.WindowBrushKey}}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header with current branch info -->
        <Border Grid.Row="0" Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" 
                Padding="10" BorderBrush="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}" 
                BorderThickness="0,0,0,1">
            <StackPanel>
                <TextBlock Text="GitFlow for Visual Studio" FontSize="16" FontWeight="Bold" 
                          Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" Margin="0,0,0,5"/>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Current Branch: " 
                              Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
                    <TextBlock x:Name="CurrentBranchText" Text="(loading...)" FontWeight="Bold" 
                              Foreground="{DynamicResource {x:Static SystemColors.HotTrackBrushKey}}"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Main content area -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="MainContent" Margin="10">
                <!-- Content will be loaded dynamically based on GitFlow state -->
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
