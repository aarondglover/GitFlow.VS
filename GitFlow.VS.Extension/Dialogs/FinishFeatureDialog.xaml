<Window x:Class="GitFlowVS.Extension.Dialogs.FinishFeatureDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vsshell="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0"
        Title="Finish Feature" 
        Height="220" Width="480"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource {x:Static vsshell:VsBrushes.WindowKey}}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Finish Feature" FontSize="14" FontWeight="Bold" Margin="0,0,0,15"
                  Foreground="{DynamicResource {x:Static vsshell:VsBrushes.WindowTextKey}}"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,20">
            <TextBlock Text="Select Feature to Finish:" Margin="0,0,0,5"
                      Foreground="{DynamicResource {x:Static vsshell:VsBrushes.WindowTextKey}}"/>
            <ComboBox x:Name="FeatureComboBox" Padding="5" Height="26" DisplayMemberPath="." />
            <TextBlock Text="The feature will be merged into develop and the branch will be deleted" 
                       FontSize="10" FontStyle="Italic" Margin="0,5,0,0"
                       Foreground="{DynamicResource {x:Static vsshell:VsBrushes.GrayTextKey}}"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Finish" Width="80" Height="28" Margin="0,0,10,0" IsDefault="True" Click="OkButton_Click"/>
            <Button Content="Cancel" Width="80" Height="28" IsCancel="True" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
